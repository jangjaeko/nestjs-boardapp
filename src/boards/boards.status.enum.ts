// export interface Board {
//   id: string;
//   title: string;
//   description: string;
//   status: BoardStatus;
// }
//for local

export enum BoardStatus {
  PRIVATE = 'PRIVATE',
  PUBLIC = 'PUBLIC',
}
